$wallpapers = $HOME/Documents/WorkFlow/envCross_dotfiles/Linux-config/hyprlock/wallpapers
$font = JetBrainsMono Nerd Font
$icon_font = Symbols Nerd Font

background {
    monitor =
    path = $wallpapers/16.jpeg
    blur_passes = 0
    contrast = 0.8916
    brightness = 0.8172
    vibrancy = 0.0
    vibrancy_darkness = 0.0
}


shape {
    monitor =
    size = 306, 323
    color = rgba(0, 0, 0, 0.3)
    rounding = 25
    border_size = 0
    position = 400, 130
    halign = center
    valign = center
    zindex = 1
}

label {
    monitor =
    text = cmd[update:1000] hostname
    color = rgba(185, 175, 165, 1.0)
    font_size = 12
    font_family = $font
    position = 400, 250
    halign = center
    valign = center
    zindex = 2
}

label {
    monitor =
    text = Log in
    color = rgba(240, 230, 220, 1.0)
    font_size = 36
    font_family = $font
    position = 400, 198
    halign = center
    valign = center
    zindex = 2
}

shape {
    monitor =
    size = 272, 60
    color = rgba(186, 61, 61, 1.0)
    rounding = 20
    position = 400, 105
    halign = center
    valign = center
    zindex = 2
}

label {
    monitor =
    text = username
    color = rgba(25, 20, 20, 1.0)
    font_size = 10
    font_family = $font
    position = 307, 120
    halign = center
    valign = center
    zindex = 3
}

label {
    monitor =
    text = cmd[update:1000] echo "<b>$USER</b>"
    color = rgba(25, 20, 20, 1.0)
    font_size = 15
    font_family = $font
    position = 296, 94
    halign = center
    valign = center
    zindex = 3
}

shape {
    monitor =
    size = 272, 60
    color = rgba(186, 61, 61, 1.0)
    rounding = 20
    position = 400, 20
    halign = center
    valign = center
    zindex = 2
}

label {
    monitor =
    text = password
    color = rgba(25, 20, 20, 1.0)
    font_size = 10
    font_family = $font
    position = 307, 35
    halign = center
    valign = center
    zindex = 4
}

input-field {
    monitor =
    size = 240, 28
    outline_thickness = 0
    dots_size = 0.25
    dots_spacing = 0.4
    dots_center = false
    outer_color = rgba(0, 0, 0, 0)
    inner_color = rgba(0, 0, 0, 0)
    font_color = rgba(25, 20, 20, 1.0)
    fade_on_empty = false
    placeholder_text =
    hide_input = false
    rounding = 0
    position = 385, 9
    halign = center
    valign = center
    zindex = 3
}

shape {
    monitor =
    size = 306, 136
    color = rgba(0, 0, 0, 0.3)
    rounding = 25
    position = 400, -140
    halign = center
    valign = center
    zindex = 1
}

label {
    monitor =
    text = cmd[update:1000] date +"<b>%H:%M</b>"
    color = rgba(240, 230, 220, 1.0)
    font_size = 60
    font_family = $font
    position = 400, -128
    halign = center
    valign = center
    zindex = 2
}

label {
    monitor =
    text = cmd[update:1000] uptime -p | sed -e 's/up /  UPTIME /' -e 's/ hours, /H/' -e 's/ minutes/M/' -e 's/ hour, /H/' -e 's/ minute/M/'
    color = rgba(185, 175, 165, 1.0)
    font_size = 12
    font_family = $font
    position = 400, -188
    halign = center
    valign = center
    zindex = 2
}

shape {
    monitor =
    size = 306, 323
    color = rgba(0, 0, 0, 0.3)
    rounding = 25
    position = 730, 130
    halign = center
    valign = center
    zindex = 1
}

image {
    monitor =
    path = $wallpapers/engineer.png
    size = 102
    rounding = 20
    border_size = 4
    border_color = rgba(186, 61, 61, 1.0)
    position = 730, 220
    halign = center
    valign = center
    zindex = 2
}

label {
    monitor =
    text = cmd[update:1000] echo "<b>$(date +"%a" | tr '[:lower:]' '[:upper:]')</b>"
    color = rgba(240, 230, 220, 1.0)
    font_size = 32
    font_family = $font
    position = 730, 122
    halign = center
    valign = center
    zindex = 2
}

label {
    monitor =
    text = cmd[update:1000] date +"%d %B, %Y"
    color = rgba(185, 175, 165, 1.0)
    font_size = 12
    font_family = $font
    position = 730, 78
    halign = center
    valign = center
    zindex = 11
}

shape {
    monitor =
    size = 306, 272
    color = rgba(35, 31, 31, 0.8)
    rounding = 25
    position = 730, -140
    halign = center
    valign = center
    shadow_passes = 2
    shadow_size = 6
    shadow_color = rgba(0, 0, 0, 0.5)
    zindex = 10
}

label {
    monitor =
    text = cmd[update:1000] player=$(playerctl -l 2>/dev/null | head -n 1); if [ "$player" = "spotify" ]; then echo "󰓇"; elif [ -n "$player" ]; then echo "󰗃"; else echo "󰎆"; fi
    color = rgba(240, 230, 220, 1.0)
    font_size = 15
    font_family = $icon_font
    position = 730, -30
    halign = center
    valign = center
    zindex = 11
}

label {
    monitor =
    text = cmd[update:1000] title=$(playerctl metadata xesam:title 2>/dev/null | cut -c1-24); if [ -z "$title" ]; then echo "No Music Playing"; else echo "$title"; fi
    color = rgba(240, 230, 220, 1.0)
    font_size = 12
    font_family = $font
    position = 730, -55
    halign = center
    valign = center
    zindex = 11
}

label {
    monitor =
    text = cmd[update:1000] artist=$(playerctl metadata xesam:artist 2>/dev/null | cut -c1-30); if [ -z "$artist" ]; then echo ""; else echo "$artist"; fi
    color = rgba(185, 175, 165, 1.0)
    font_size = 12
    font_family = $font
    position = 730, -73
    halign = center
    valign = center
    zindex = 11
}

shape {
    monitor =
    size = 128, 64
    color = rgba(204, 164, 124, 1.0)
    rounding = 20
    position = 658, -128
    halign = center
    valign = center
    zindex = 11
}

label {
    monitor =
    text = 󰤨
    color = rgba(25, 20, 20, 1.0)
    font_size = 15
    font_family = $icon_font
    position = 658, -120
    halign = center
    valign = center
    zindex = 12
}

label {
    monitor =
    text = cmd[update:1000] ssid=$(iw dev 2>/dev/null | awk '$1=="ssid"{$1=""; sub(/^ /, ""); print; exit}'); if [ -z "$ssid" ]; then echo "Offline"; else printf "%b" "$ssid" | cut -c1-14; fi
    color = rgba(25, 20, 20, 1.0)
    font_size = 10
    font_family = $font
    position = 658, -142
    halign = center
    valign = center
    zindex = 12
}

shape {
    monitor =
    size = 128, 64
    color = rgba(204, 164, 124, 1.0)
    rounding = 20
    position = 802, -128
    halign = center
    valign = center
    zindex = 11
}

label {
    monitor =
    text = 󰂯
    color = rgba(25, 20, 20, 1.0)
    font_size = 15
    font_family = $icon_font
    position = 802, -120
    halign = center
    valign = center
    zindex = 12
}

label {
    monitor =
    text = cmd[update:1000] bt=$(bluetoothctl info 2>/dev/null | awk -F': ' '/Name:/{print $2; exit}'); if [ -z "$bt" ]; then echo "Offline"; else printf "%s" "$bt" | cut -c1-14; fi
    color = rgba(25, 20, 20, 1.0)
    font_size = 10
    font_family = $font
    position = 802, -142
    halign = center
    valign = center
    zindex = 12
}

shape {
    monitor =
    size = 272, 85
    color = rgba(186, 61, 61, 1.0)
    rounding = 25
    position = 730, -220
    halign = center
    valign = center
    zindex = 11
}

shape {
    monitor =
    size = 85, 17
    color = rgba(25, 20, 20, 1.0)
    rounding = 5
    position = 730, -195
    halign = center
    valign = center
    zindex = 12
}

label {
    monitor =
    text = cmd[update:1000] status=$(cat /sys/class/power_supply/BAT0/status 2>/dev/null); if [ "$status" = "Discharging" ]; then echo " ! "; else echo " 󰚥 "; fi
    color = rgba(186, 61, 61, 1.0)
    font_size = 12
    font_family = $font
    position = 730, -195
    halign = center
    valign = center
    zindex = 13
}

label {
    monitor =
    text = cmd[update:1000] capacity=$(cat /sys/class/power_supply/BAT0/capacity 2>/dev/null); if [ -z "$capacity" ]; then echo "--%"; else echo "<b>$capacity%</b>"; fi
    color = rgba(25, 20, 20, 1.0)
    font_size = 20
    font_family = $font
    position = 730, -220
    halign = center
    valign = center
    zindex = 12
}

label {
    monitor =
    text = cmd[update:1000] status=$(cat /sys/class/power_supply/BAT0/status 2>/dev/null); if [ -z "$status" ]; then echo "No battery"; else echo "$status"; fi
    color = rgba(25, 20, 20, 1.0)
    font_size = 10
    font_family = $font
    position = 730, -246
    halign = center
    valign = center
    zindex = 12
}
