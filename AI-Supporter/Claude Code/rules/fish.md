---
paths:
  - "**/*.fish"
---

# Fish Rules

- Prefer simple Fish-native syntax.
- Use `function --description` only when a description is required.
- Avoid comments unless they carry non-obvious information.
