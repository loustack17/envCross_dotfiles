---
description: Compress a Markdown/text memory file with Caveman compression.
---

Use the `compress` skill on this target:

`$ARGUMENTS`

If no target path is provided, ask for exactly one path. Do not infer a file.
